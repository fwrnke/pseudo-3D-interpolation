[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.pydocstringformatter]
style = "numpydoc"
max-line-length = 120

[tool.black]
line-length = 120
skip-string-normalization = true
